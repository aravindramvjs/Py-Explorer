# tuple is also like list, but it is unmutable.
# instead of square bracket here we use (set bracket)
# the iteration rate is fast than list in tuple

mytuple = (1,2,3,4,5)
print(mytuple)

# there only two operation in tuple count and index
print(mytuple.index(1))